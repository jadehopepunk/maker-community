<div class="admin-media-gallery">
  <div class="images">
    <% @images.each do |image| %> <% if image.file.representable? %>
    <div class="image-container"><%= image_tag image.file.variant(:thumb) %></div>
    <% end %> <% end %>
  </div>
  <%= paginate @images %>
</div>
