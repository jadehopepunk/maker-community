<div class="tag-filters">
  <% tags.each do |name, count| %>
  <div>
    <a class="tag-button with-count" href="#<%= name %>"> <span class="tag-name"><%= name.humanize %></span><span class="count"><%= count %></span> </a>
  </div>
  <% end %>
</div>
