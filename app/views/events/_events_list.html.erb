<div class="events-list <%= defined?(style) ? style : 'default' %>">
  <% event_sessions.each do |event_session| %> <%= render "events/event_session_preview", event_session: event_session %> <% end %>
</div>
